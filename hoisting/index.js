
var nameOfDog; // undefined, creado automaticamente si se imprime y luego declara

console.log(nameOfDog);
var nameOfDog = 'Elmo';
console.log(nameOfDog);

var elmo; // undefined, creado automaticamente si se imprime y luego declara
nombreDePerrito();

function nombreDePerrito(){
    console.log('El mejor perrito es ' + elmo);
}

var elmo = 'Elmito';